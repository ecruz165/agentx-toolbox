# GitHub token (OPTIONAL — gittyup auto-detects from auth.json, `gh auth`, and git credentials)
# Only set this if you want to override the auto-detected token.
# GITHUB_TOKEN="ghp_..."

# No separate AI API key needed — run `gittyup auth login` to use your Copilot subscription.

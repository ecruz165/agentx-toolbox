{
  "name": "agentx-toolbox",
  "version": "0.0.0",
  "description": "AgentX Toolbox — a coordinated set of CLI tools for the AgentX ecosystem.",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:watch": "npm run test:watch --workspace=@agentx/skillzkit",
    "catalog": "npm run catalog --workspace=@agentx/skillzkit",
    "skillzkit": "tsx apps/skillzkit/bin/cli.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ecruz165/agentx-toolbox.git"
  },
  "license": "UNLICENSED"
}
